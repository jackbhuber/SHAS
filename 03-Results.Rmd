
```{r, include=FALSE, echo=FALSE}
knitr::opts_chunk$set(comment = "", warning = FALSE, echo = FALSE,  message = FALSE, tidy.opts=list(width.cutoff=60), tidy = TRUE)
knitr::opts_knit$set(verbose = FALSE)
```

# Results

```{r data-prep, include=TRUE, echo=FALSE}

# LOAD ORIGINAL DATA
library(readxl) # for reading the source Excel file
shas <- read_excel("data/data.xlsx") # load Excel file

# SELECT ITEMS
all1 <- shas[ ,14:97] # select items, dems from columns 14-79
all1 <- subset(all1, select = -c(Ext_Avg:Global_Avg)) # remove columns for now
# items <- all[ ,1:66]

# DEAL WITH MISSING DATA
library(naniar)
library(tidyverse)
miss <- miss_case_summary(all1)
miss <- miss[order(miss$case),]
all2 <- cbind(all1, miss)
all <- subset(all2, pct_miss <= 50)
nonresp <- subset(all2, pct_miss > 50)
rm(all1,all2,miss,nonresp,shas)
```

## Participant Characteristics

```{r, child = "_ParticipantCharacteristics.Rmd"}
```

## Initial Item Analysis

```{r, child = "_InitialItemAnalysis.Rmd"}
```

## Exploratory Bifactor Analysis

```{r, child = "_Dimensionality_ExploratoryBifactor.Rmd"}
```

## Bifactor Model of Final XX-Item Set

## Item Response Theory Model

```{r}
```

## DIF Analysis

```{r, child = "_DIF.Rmd"}
```
